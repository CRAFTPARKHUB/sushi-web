import siteMetadata from "../siteMetadata/siteMetadata";

const metadata = (params) => ({bodyClassName: "framer-body-QzDvm9nD_", breakpoints: [{hash: "9jzick"}], elements: {}, title: siteMetadata(params).title || "Page", viewport: "width=1200"});
export default metadata;

export const metadataVersion = 1;
