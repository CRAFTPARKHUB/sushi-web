import siteMetadata from "../siteMetadata/siteMetadata";

const metadata = (params) => ({bodyClassName: "framer-body-KBlFSas6i", breakpoints: [{hash: "1uesjik"}], elements: {}, robots: "noindex", title: siteMetadata(params).title || "404", viewport: "width=1280"});
export default metadata;

export const metadataVersion = 1;
