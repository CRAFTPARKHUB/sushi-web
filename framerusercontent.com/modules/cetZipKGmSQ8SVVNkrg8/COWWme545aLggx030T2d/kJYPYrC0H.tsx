import siteMetadata from "../siteMetadata/siteMetadata";

const metadata = (params) => ({bodyClassName: "framer-body-kJYPYrC0H", breakpoints: [{hash: "zbijjt"}], elements: {OuAtiqQLc: "content"}, title: siteMetadata(params).title || "Page", viewport: "width=1920"});
export default metadata;

export const metadataVersion = 1;
