import siteMetadata from "../siteMetadata/siteMetadata";

const metadata = (params) => ({bodyClassName: "framer-body-tBpZJGMw3", breakpoints: [{hash: "15or55y"}], elements: {}, title: siteMetadata(params).title || "Work", viewport: "width=1200"});
export default metadata;

export const metadataVersion = 1;
