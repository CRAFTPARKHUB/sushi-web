import siteMetadata from "../siteMetadata/siteMetadata";

const metadata = (params) => ({breakpoints: [{hash: "1l1gc90"}], elements: {}, title: siteMetadata(params).title || "About", viewport: "width=1200"});
export default metadata;

export const metadataVersion = 1;
